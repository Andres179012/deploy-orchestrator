name: Deploy Any Repository

on:
  workflow_dispatch:
    inputs:
      repo_name:
        description: "Nombre del repositorio a desplegar"
        required: true
      repo_owner:
        description: "Propietario del repositorio (usuario u organización)"
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código del repositorio centralizado
        uses: actions/checkout@v4

      - name: Clonar el repositorio a desplegar
        run: |
          git clone https://${{ secrets.PERSONAL_ACCESS_TOKEN }}@github.com/${{ github.event.inputs.repo_owner }}/${{ github.event.inputs.repo_name }}.git

      - name: Definir variable de entorno en minúsculas
        run: |
          echo "GITHUB_REPO_NAME=$(echo ${{ github.event.inputs.repo_name }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Detectar framework y configurar Astro
        run: |
          cd ${{ github.event.inputs.repo_name }}

          PORT=3000
          START_CMD="npm run start"

          # Detectar el framework según package.json
          if grep -q '"astro"' package.json; then
            PORT=4321
            START_CMD="npm run preview -- --host"

            # 🔥 1️⃣ Eliminar Netlify correctamente
            if grep -q '@astrojs/netlify' astro.config.mjs; then
              echo "🚀 Eliminando Netlify del archivo de configuración..."
              sed -i '/import netlify/d' astro.config.mjs
              sed -i '/adapter: netlify/d' astro.config.mjs
              echo "✅ Netlify eliminado correctamente"
            fi

            # 🔥 2️⃣ Si output: 'server' está presente, asegurar adaptador Node.js
            if grep -q "output: 'server'" astro.config.mjs && ! grep -q 'adapter:' astro.config.mjs; then
              echo "🚀 Instalando y configurando el adaptador de Node.js..."

              # Asegurar que @astrojs/node está instalado
              npm install @astrojs/node --save-dev

              # Agregar import si falta
              if ! grep -q '@astrojs/node' astro.config.mjs; then
                sed -i '1 i\import node from "@astrojs/node";' astro.config.mjs
              fi

              # Agregar adaptador dentro de defineConfig si no está
              sed -i '/defineConfig({/a\  adapter: node(),' astro.config.mjs

              echo "✅ Adaptador de Node.js configurado correctamente"
            fi

            # 🔧 3️⃣ Corregir sintaxis si quedó rota
            if grep -q '  )});' astro.config.mjs; then
              echo "⚠️ Corrigiendo sintaxis en astro.config.mjs..."
              sed -i 's/^  )});/  });/' astro.config.mjs
              echo "✅ Sintaxis corregida"
            fi
          fi

          echo "🔍 Framework detectado: $START_CMD con puerto: $PORT"

          # 🚀 4️⃣ Forzar instalación de dependencias antes del build
          echo "🚀 Instalando todas las dependencias antes del build..."
          npm install --legacy-peer-deps

          # Crear Dockerfile
          cat <<EOF > Dockerfile
          FROM node:18-alpine
          WORKDIR /app
          COPY package*.json ./  
          RUN npm install --legacy-peer-deps
          COPY . .
          RUN npm run build || echo 'No se necesita build'
          EXPOSE $PORT
          CMD ["sh", "-c", "$START_CMD"]
          EOF

          echo "PORT=$PORT" >> $GITHUB_ENV

      - name: Crear archivo deployment.yaml
        run: |
          cd ${{ github.event.inputs.repo_name }}
          mkdir -p k8s
          cat <<EOF > k8s/deployment.yaml
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: ${GITHUB_REPO_NAME}
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: ${GITHUB_REPO_NAME}
            template:
              metadata:
                labels:
                  app: ${GITHUB_REPO_NAME}
              spec:
                containers:
                  - name: ${GITHUB_REPO_NAME}
                    image: registry.digitalocean.com/cosmly-registry/${GITHUB_REPO_NAME}:latest
                    ports:
                      - containerPort: ${PORT}
                    imagePullPolicy: Always
                    env:
                      - name: PORT
                        value: "${PORT}"
          --- 
          apiVersion: v1
          kind: Service
          metadata:
            name: ${GITHUB_REPO_NAME}-service
          spec:
            selector:
              app: ${GITHUB_REPO_NAME}
            ports:
              - protocol: TCP
                port: 80
                targetPort: ${PORT}
            type: LoadBalancer
          EOF

      - name: Iniciar sesión en DigitalOcean
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Iniciar sesión en el Container Registry
        run: doctl registry login

      - name: Construir y subir imagen Docker
        run: |
          cd ${{ github.event.inputs.repo_name }}
          docker build -t registry.digitalocean.com/cosmly-registry/${GITHUB_REPO_NAME}:latest .
          docker push registry.digitalocean.com/cosmly-registry/${GITHUB_REPO_NAME}:latest

      - name: Configurar kubectl
        run: doctl kubernetes cluster kubeconfig save k8s-1-32-1-do-0-nyc1-1740853375666

      - name: Validar deployment.yaml
        run: |
          cd ${{ github.event.inputs.repo_name }}
          kubectl apply --dry-run=client -f k8s/deployment.yaml

      - name: Desplegar en Kubernetes
        run: |
          cd ${{ github.event.inputs.repo_name }}
          kubectl apply -f k8s/deployment.yaml
          kubectl rollout restart deployment/${GITHUB_REPO_NAME}
