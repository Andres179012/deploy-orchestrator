name: Deploy Any Repository

on:
  workflow_dispatch:
    inputs:
      repo_name:
        description: "Nombre del repositorio a desplegar"
        required: true
      repo_owner:
        description: "Propietario del repositorio (usuario u organización)"
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código del repositorio centralizado
        uses: actions/checkout@v4

      - name: Clonar el repositorio a desplegar
        run: |
          git clone https://${{ secrets.PERSONAL_ACCESS_TOKEN }}@github.com/${{ github.event.inputs.repo_owner }}/${{ github.event.inputs.repo_name }}.git

      - name: Definir variable de entorno en minúsculas
        run: |
          echo "GITHUB_REPO_NAME=$(echo ${{ github.event.inputs.repo_name }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      # 🔥 Instalar dependencias y asegurar versión compatible de `@astrojs/node`
      - name: Instalar dependencias y `@astrojs/node`
        run: |
          cd ${{ github.event.inputs.repo_name }}
          echo "🚀 Instalando todas las dependencias..."
          npm install --legacy-peer-deps

          # Detectar la versión de Astro en package.json
          ASTRO_VERSION=$(node -p "require('./package.json').dependencies?.astro || require('./package.json').devDependencies?.astro || ''")
          
          # Si el proyecto usa Astro, instalar `@astrojs/node` con la versión compatible
          if [[ "$ASTRO_VERSION" =~ ^3 ]]; then
            echo "🚀 Detectado Astro $ASTRO_VERSION, instalando @astrojs/node@4..."
            npm install @astrojs/node@4 --save-dev --legacy-peer-deps
          elif [[ "$ASTRO_VERSION" =~ ^4 ]]; then
            echo "🚀 Detectado Astro $ASTRO_VERSION, instalando @astrojs/node@8..."
            npm install @astrojs/node@8 --save-dev --legacy-peer-deps
          elif [[ "$ASTRO_VERSION" =~ ^5 ]]; then
            echo "🚀 Detectado Astro $ASTRO_VERSION, instalando @astrojs/node@9..."
            npm install @astrojs/node@9 --save-dev --legacy-peer-deps
          else
            echo "⚠️ No se pudo detectar la versión de Astro, usando @astrojs/node más reciente"
            npm install @astrojs/node --save-dev --legacy-peer-deps
          fi
          
          echo "✅ Dependencias instaladas correctamente"

      # 🔥 Crear script para corregir astro.config.mjs
      - name: Crear script para corregir astro.config.mjs
        run: |
          cat <<EOF > fix-astro-config.js
          const fs = require('fs');

          const filePath = './astro.config.mjs';
          if (!fs.existsSync(filePath)) {
            console.log('❌ No se encontró astro.config.mjs');
            process.exit(0);
          }

          let content = fs.readFileSync(filePath, 'utf8');

          // 🔥 1️⃣ Eliminar Netlify sin romper la sintaxis
          content = content.replace(/import netlify.*\n?/g, '');
          content = content.replace(/adapter:\s*netlify\(.*?\),?/gs, '');

          // 🔥 2️⃣ Asegurar que la configuración base de Astro está bien definida
          if (!content.includes("defineConfig")) {
            console.log("❌ Error: No se encontró defineConfig en astro.config.mjs");
            process.exit(1);
          }

          // 🔥 3️⃣ Agregar `import node from '@astrojs/node'` si falta
          if (!content.includes("import node from '@astrojs/node'")) {
            content = "import node from '@astrojs/node';\n" + content;
          }

          // 🔥 4️⃣ Agregar `output: 'server'` si falta
          if (!content.includes("output: 'server'")) {
            content = content.replace(/defineConfig\(\{\s*/, "defineConfig({\n  output: 'server',\n");
          }

          // 🔥 5️⃣ Agregar el adaptador de Node.js correctamente
          if (!content.includes("adapter: node({ mode: 'middleware' })")) {
            content = content.replace(/defineConfig\(\{\s*/, "defineConfig({\n  adapter: node({ mode: 'middleware' }),\n");
          }

          // 🔧 6️⃣ Corregir errores de cierre de llaves `})`
          content = content.replace(/\)\}\);/g, '});');

          fs.writeFileSync(filePath, content, 'utf8');
          console.log('✅ astro.config.mjs actualizado correctamente');
          EOF

      # 🔥 Ejecutar script de corrección antes del build
      - name: Corregir Astro antes del build
        run: |
          cd ${{ github.event.inputs.repo_name }}
          node ../fix-astro-config.js

      - name: Crear Dockerfile
        run: |
          cd ${{ github.event.inputs.repo_name }}
          cat <<EOF > Dockerfile
          FROM node:18-alpine
          WORKDIR /app
          COPY package*.json ./  
          RUN npm install --legacy-peer-deps
          COPY . .
          RUN npm run build || echo 'No se necesita build'
          EXPOSE 4321
          CMD ["sh", "-c", "npm run preview -- --host"]
          EOF

      - name: Crear archivo deployment.yaml
        run: |
          cd ${{ github.event.inputs.repo_name }}
          mkdir -p k8s
          cat <<EOF > k8s/deployment.yaml
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: ${GITHUB_REPO_NAME}
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: ${GITHUB_REPO_NAME}
            template:
              metadata:
                labels:
                  app: ${GITHUB_REPO_NAME}
              spec:
                containers:
                  - name: ${GITHUB_REPO_NAME}
                    image: registry.digitalocean.com/cosmly-registry/${GITHUB_REPO_NAME}:latest
                    ports:
                      - containerPort: 4321
                    imagePullPolicy: Always
                    env:
                      - name: PORT
                        value: "4321"
          --- 
          apiVersion: v1
          kind: Service
          metadata:
            name: ${GITHUB_REPO_NAME}-service
          spec:
            selector:
              app: ${GITHUB_REPO_NAME}
            ports:
              - protocol: TCP
                port: 80
                targetPort: 4321
            type: LoadBalancer
          EOF

      - name: Iniciar sesión en DigitalOcean
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Iniciar sesión en el Container Registry
        run: doctl registry login

      - name: Construir y subir imagen Docker
        run: |
          cd ${{ github.event.inputs.repo_name }}
          docker build -t registry.digitalocean.com/cosmly-registry/${GITHUB_REPO_NAME}:latest .
          docker push registry.digitalocean.com/cosmly-registry/${GITHUB_REPO_NAME}:latest

      - name: Configurar kubectl
        run: doctl kubernetes cluster kubeconfig save k8s-1-32-1-do-0-nyc1-1740853375666

      - name: Validar deployment.yaml
        run: |
          cd ${{ github.event.inputs.repo_name }}
          kubectl apply --dry-run=client -f k8s/deployment.yaml

      - name: Desplegar en Kubernetes
        run: |
          cd ${{ github.event.inputs.repo_name }}
          kubectl apply -f k8s/deployment.yaml
          kubectl rollout restart deployment/${GITHUB_REPO_NAME}
