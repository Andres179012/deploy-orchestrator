apiVersion: v1
kind: Service
metadata:
  name: djhomeremodelersinc-seo-service
  namespace: default
spec:
  type: ClusterIP  # 🔥 Cambiado de LoadBalancer a ClusterIP
  selector:
    app: djhomeremodelersinc-seo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4321
